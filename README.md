Install instructions and reqs:

- install the Anaconda python distribution.
- install the dataset in HTMLS/ (not in the repo due to size)
- pip3 install pyLDAvis
- python nltk_installs.py
- python run.py
